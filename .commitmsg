fix: audio capture, yellow border, and add recording overlay

- Use Chromium loopback for audio (captures all system audio) instead
  of VB-Cable which requires manual routing
- Stop video tracks in audio-only loopback to eliminate yellow border
- Add always-on-top overlay notifications for recording start/stop/save
- Toast notifications in renderer for recording state changes
